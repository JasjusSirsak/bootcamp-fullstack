const validator = require('validator');

const email = 'bonito@gmail.com'
const checkEmail = validator .isEmail(email);

const email = 'bonito@gmail.com'
const checkEmail = validator .isEmail(email);
console.log(checkEmail);
